# secure-multi-tenant-api-platform
Spring Boot 3.5 (Java 21) API platform enforcing tenant isolation with JWT tenantId claim, PostgreSQL Row-Level Security (RLS) to eliminate “missing tenant filter” mistakes, and tenant-aware Redis cache keys
